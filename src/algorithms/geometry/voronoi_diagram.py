from src.structures import Point
from typing import List


def fortune():
    """
    Fortune's algorithm is a sweep line algorithm for generating a Voronoi
    diagram from a set of points in a plane using O(n log n) time and O(n) space.
    """

